- name : Configure Docker on EC2 Instances
  hosts : localhost
  connection : ssh
  tasks : 
  - name: Deploy Docker Container
    command: docker run -itd -p 8084:8081 your-dockerhub-username/asi-insure:1.0
